services:
  node:
    image: node:22-alpine
    container_name: iot
    ports:
      - "8080:8080"
    volumes:
      - ".:/home/node/app"
    working_dir: /home/node/app
    command: /bin/sh -c "npm install && node bin/www"
